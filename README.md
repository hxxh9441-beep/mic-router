# 🎙️ مكبّر الصوت — حوّل جوالك إلى مايكروفون مع فلاتر وتسجيل

تطبيق ويب صفحة واحدة يحوّل جهازك إلى **مكبّر صوت حي**: يلتقط الميكروفون ويرسله مباشرة إلى السماعات (سلكية أو بلوتوث) مع فلاتر لحظية وتسجيل اختياري — **PWA كاملة** تعمل كتطبيق مستقل من شاشتك الرئيسية.

## ✨ المميزات

- 🎙️ **توجيه مباشر**: ميكروفون → سماعات بدون تخزين وسيط (تأخير منخفض `latencyHint: 'interactive'`)
- 🎛️ **3 فلاتر حية**: قطع الضوضاء المنخفضة (High-Pass 20–500Hz) · الحساسية (Gain 0–2x) · صدى قابل للضبط
- 🎚️ **تحكم دقيق**: كل فلتر عنده حقل نسبة قابل للكتابة + أزرار ±5% + قيم سريعة [25|50|75|100]
- 🔴 **تسجيل الجلسة**: MediaRecorder يلتقط الصوت **بعد** الفلاتر (اللي تسمعه هو اللي يتسجل) + مؤقت حي
- ⏹️ **إيقاف ذكي**: حفظ تلقائي للتسجيل حتى لو أوقفت الجلسة أو عطّلت التسجيل أثناء العمل
- 💾 **تنزيل فوري**: زر «تنزيل التسجيل» بعد كل تسجيل (webm / m4a / ogg حسب المتصفح)
- 📊 **فيجوالايزر** حي (طيف الترددات) عبر AnalyserNode
- 📱 **PWA كاملة**: تثبيت من الشاشة الرئيسية (Android + iPhone) + تشغيل أوفلاين

## 🎛️ سلسلة المعالجة

```
الميكروفون (getUserMedia — الإعدادات حسب المنصة)
  → High-Pass Filter (20–500Hz)      ← قص الهمهمة المنخفضة
  → Gain (0–2x)                      ← الحساسية
  → [ Master Gain ] → السماعات مباشرة            ← متصلة دائماً
       ├─ → حلقة الصدى: wet → Delay (0–1s) → feedback → السماعات
       └─ → MediaStreamDestination   ← التسجيل (جاف + رطب)
            └─ → MediaRecorder → Blob → مشغّل + تنزيل
```

- **مسار جاف مباشر**: `الحساسية ← السماعات` متصل **دائماً** حتى لو كل المؤثرات 0%.
- **استئناف مبكر**: `resume()` يُستدعى فور الضغط — قبل طلب الميكروفون — ضمن إيماءة المستخدم.
- **تحصين القيم**: كل الفلاتر ترفض NaN/النصوص وتُقلم لحدودها (الحساسية لا تنزل عن 1.0 الافتراضية).

## 📱 توافق الجوال (iOS Safari + Android)

مشكلة معروفة على الجوال: الميكروفون يلتقط (الأيقونة الخضراء) لكن لا يخرج صوت من السماعات.
السبب الموثق: **Safari يعلّق الـ AudioContext أثناء نافذة إذن الميكروفون** ([SAFARI_WEBAUIO_QUIRKS](https://github.com/msitarzewski/openstudio/blob/main/docs/SAFARI_WEBAUIO_QUIRKS.md) ·
[WebKit bug 261554](https://bugs.webkit.org/show_bug.cgi?id=261554))، والعقد المبنية في سياق معلّق
لا تعالج الصوت إطلاقاً. المعالجة في هذا المشروع:

1. **🔓 فتح iOS (`iosAudioUnlock`)**: سياق مؤقت يُستأنف في **أول سطر** من النقرة — يقلب عداد
   «has succeeded» عند iOS فيقبل كل الاستئنافات اللاحقة (حتى بعد نافذة الإذن).
2. **🔑 استئناف حاسم بعد الإذن**: `start()` يستدعي `resume()` **بعد** `getUserMedia` و**قبل** بناء
   العقد — فالسياق يعمل قبل إنشاء أي عقدة (العقد في سياق معلّق = صمت).
3. **⏱️ حد زمني آمن**: `_resumeCtx()` يتسابق مع مؤقت 1200ms — لا يعلّق `start()` أبداً إذا رفضت
   السياسة الاستئناف.
4. **♻️ استئناف مستمر (keep-alive)**: أي نقرة/لمسة أو عودة للتبويب من الخلفية تعيد محاولة
   الاستئناف تلقائياً (iOS يعيد التعليق أحياناً عند عودة التبويب).

### أندرويد (Chrome)

مشكلة مختلفة جذرياً: طلب الميكروفون يدفع مسار الصوت عند كثير من أجهزة أندرويد إلى **وضع «الاتصال»** —
فيتوجَّه الصوت إلى **سماعة الأذن** (العلوية) بصوت منخفض بدل السماعة الخارجية، وتتحكم أزرار الصوت
بدرجة «المكالمة» ([Google Issue 443255997](https://issuetracker.google.com/issues/443255997) ·
["getUserMedia() mutes audio output in Chrome for Mobile"](https://stackoverflow.com/questions/31054840)).

**السبب الجذري (مؤكد من كود Chromium)**: `getUserMedia` يستدعي
`setCommunicationAudioModeOn(true)` في `AudioManagerAndroid.java` → يفرض
`AudioManager.MODE_IN_COMMUNICATION` على مستوى النظام — فكل مخرجات WebAudio
(`ctx.destination` أو عنصر `<audio>`) تُوجَّه لسماعة الأذن أو تُكتم، وتتحكم
أزرار الصوت بدرجة «المكالمة» ([Google Issue 443255997](https://issuetracker.google.com/issues/443255997)).
`setSinkId` لا يفيد (المتصفح يعرض «System Default (Browser Limited)»).

الحل المطبق — **WebRTC loopback محلي**:

1. **🔁 مسار WebRTC (`_setupLoopback`)**: الصوت المعالج يُمرَّر عبر
   `RTCPeerConnection` محلي (متصلان، بلا خوادم ICE) ويُشغَّل عبر عنصر `<audio>` —
   مسار «المكالمة» الخارجي الذي يخرج من السماعة الخارجية على أندرويد (كما في
   مكالمات Meet). إن تعذّر → fallback للمخرج المباشر.
2. **🎚️ الوضع الخام**: `echoCancellation/noiseSuppression/autoGainControl: false`.
3. **🔊 فرض سماعة الوسائط (`_forceMediaSink`)**: `AudioContext.setSinkId('')` —
   مدعوم على أندرويد كروم 110+ (خلافاً لـ `HTMLMediaElement.setSinkId`).
4. **⏳ مراقب صمت تلقائي**: إن كان الإدخال صامتاً تماماً بعد 1.5 ثانية، يبدّل
   المراقب إلى الافتراضيات (`true/true/true`) ويعيد التشغيل — جولة واحدة، لا صمت أبدي.

- **iOS/سطح المكتب**: `false/false/false` عبر `ctx.destination` مباشرة (مؤكد العمل).

للتحقق على جهازك: سطر التشخيص الحي يعرض
`ctx:.. · in:..% · out:webrtc-loopback|ctx.destination · mode:raw|defaults · sink:ok|na`،
و`Router.diagnose()` يعرض التفاصيل كاملة (حالة السياق، الوضع، نتيجة فرض السماعة، المسار الجاف).

## 📱 PWA (تثبيت + أوفلاين)

- `manifest.webmanifest` — الاسم «مكبّر الصوت»، وضع مستقل، أيقونات 192/512.
- `sw.js` — شبكة أولاً للصفحة (التحديثات تصل فوراً) + كاش أولاً للأصول.
- شريط تثبيت سفلي: **Android/Chrome** (زر «تثبيت» عبر `beforeinstallprompt`) و**iPhone** (تلميح «إضافة إلى الشاشة الرئيسية»).
- إهمال المستخدم للشريط يُحفظ ولا يظهر مرة أخرى.

## 🛠️ التقنية

| الطبقة | التقنية |
|---|---|
| البناء | ملفات ثابتة بدون خطوات بناء — جاهزة للنشر المباشر |
| التنسيق | **Tailwind CSS** (CDN) + وضع داكن + RTL عربي + متجاوب |
| الصوت | Web Audio API (AudioNodes) + getUserMedia |
| التسجيل | MediaRecorder (opus/webm · m4a · ogg حسب المتصفح) |
| الاختبارات | Node.js `node:test` — تستخرج المحرك من index.html وتحاكي Web Audio |

## 🚀 التشغيل المحلي

```bash
cd "mic-router - GitHub"
python3 -m http.server 8080     # ثم افتح http://localhost:8080
node test/mic-router.test.mjs   # تشغيل اختبارات الوحدة (18/18)
```

> ملاحظة: الـ Service Worker يتطلب تشغيلاً عبر HTTP (ليس file://).

## 🌐 النشر على GitHub Pages

ملفات ثابتة — لا يحتاج بناء:

1. ارفع المشروع إلى مستودع GitHub (`git push origin main`).
2. من إعدادات المستودع: **Settings → Pages → Source: Deploy from a branch → main / root**.
3. الموقع النهائي: `https://hxxh9441-beep.github.io/mic-router/`

## 📁 بنية المشروع

```
├── index.html                 ← التطبيق كاملاً (الواجهة + المحرك + التسجيل + PWA)
├── manifest.webmanifest       ← تعريف التطبيق (تثبيت + أيقونات)
├── sw.js                      ← Service Worker (أوفلاين + تحديثات)
├── og-image.png               ← صورة المشاركة 1200×630
├── icons/                     ← أيقونات PWA (512/192 + apple-touch + favicons)
└── test/
    └── mic-router.test.mjs    ← اختبارات وحدة (node test/mic-router.test.mjs)
```

صُنع بحب للصوت الحي 🎙️✨
